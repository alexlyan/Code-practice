-- https://www.codewars.com/kata/59be9f425227ddd60c00003b/
-- SELECT prime numbers

SELECT s1.a AS prime
FROM (generate_series(2, 100) AS s1(a)
WHERE NOT EXISTS (
SELECT s2.a
FROM generate_series(2, s1.a-1) AS s2(a)
WHERE mod(s1.a, s2.a) = 0
);
